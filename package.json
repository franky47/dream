{
  "name": "dream",
  "private": true,
  "imports": {
    "#src/*": "./src/*.ts",
    "#lib/opencode/pull": "./src/lib/opencode/pull/index.ts",
    "#lib/*": "./src/lib/*.ts"
  },
  "scripts": {
    "check": "bun run fmt:check && bun run lint && bun run typecheck && bun run test && bun run knip",
    "clean:legacy-claude-sessions": "rm -rf data/raw/*/claude-sessions",
    "clean:raw": "rm -rf data/raw",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "ingest": "bun src/ingest/main.ts",
    "knip": "knip",
    "lint": "oxlint src/",
    "test": "bun test",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "errore": "^0.14.1",
    "lz4js": "^0.2.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/lz4js": "^0.2.2",
    "@types/node": "^25.6.2",
    "@typescript/native-preview": "^7.0.0-dev.20260404.1",
    "knip": "^6.12.2",
    "oxfmt": "^0.48.0",
    "oxlint": "^1.58.0",
    "oxlint-tsgolint": "^0.22.1"
  }
}
